"use strict";

Object.defineProperty(exports, "__esModule", {
    value: !0
});

var const_1 = require("../desc/const"), DBType = function() {
    function s() {}
    return s.isComplexType = function(t, c) {
        return t === this.BLOB && c === this.COMPLEX_SCALE || t === this.ARRAY || t === this.SARRAY || t === this.CLASS || t === this.PLTYPE_RECORD;
    }, s.isLocalTimeZone = function(t, c) {
        return (t === s.DATETIME || t == s.DATETIME2) && 0 != (c & s.LOCAL_TIME_ZONE_SCALE_MASK);
    }, s.getLocalTimeZoneScale = function(t, c) {
        return c & ~s.LOCAL_TIME_ZONE_SCALE_MASK;
    }, s.emptyStringToNull = function(t) {
        switch (t) {
          case s.BOOLEAN:
          case s.BIT:
          case s.TINYINT:
          case s.SMALLINT:
          case s.INT:
          case s.BIGINT:
          case s.REAL:
          case s.DOUBLE:
          case s.DECIMAL:
          case s.DATE:
          case s.TIME:
          case s.DATETIME:
          case s.DATETIME2:
          case s.INTERVAL_DT:
          case s.INTERVAL_YM:
          case s.TIME_TZ:
          case s.DATETIME_TZ:
          case s.DATETIME2_TZ:
            return !0;

          default:
            return !1;
        }
    }, s.isFloat = function(t, c) {
        return t === s.DECIMAL && c === s.ORACLE_FLOAT_SCALE_MASK;
    }, s.getFloatPrec = function(t) {
        return Math.round(.30103 * t) + 1;
    }, s.getFloatScale = function(t) {
        return t & ~s.ORACLE_FLOAT_SCALE_MASK;
    }, s.isValidBFileStr = function(t) {
        var c = t.trim().split(":");
        return 2 === c.length && !(c[0].length > const_1.Const.MAX_NAME_LEN || c[1].length > const_1.Const.MAX_PATH_LEN);
    }, s.CHAR = 0, s.VARCHAR2 = 1, s.VARCHAR = 2, s.BIT = 3, s.TINYINT = 5, s.SMALLINT = 6, 
    s.INT = 7, s.BIGINT = 8, s.DECIMAL = 9, s.REAL = 10, s.DOUBLE = 11, s.BLOB = 12, 
    s.BOOLEAN = 13, s.DATE = 14, s.TIME = 15, s.DATETIME = 16, s.BINARY = 17, s.VARBINARY = 18, 
    s.CLOB = 19, s.INTERVAL_YM = 20, s.INTERVAL_DT = 21, s.TIME_TZ = 22, s.DATETIME_TZ = 23, 
    s.XDEC_INT32 = 24, s.XDEC_INT64 = 25, s.DATETIME2 = 26, s.DATETIME2_TZ = 27, s.ROWID = 28, 
    s.NULL = 29, s.ANY = 31, s.STAR_ALL = 32, s.STAR = 33, s.RECORD = 40, s.TYPE = 41, 
    s.TYPE_REF = 42, s.UNKNOWN = 54, s.ARRAY = 117, s.CLASS = 119, s.CURSOR = 120, s.PLTYPE_RECORD = 121, 
    s.SARRAY = 122, s.CURSOR_ORACLE = -10, s.BIT_PREC = const_1.Const.BYTE_SIZE, s.TINYINT_PREC = const_1.Const.BYTE_SIZE, 
    s.SMALLINT_PREC = const_1.Const.USINT_SIZE, s.INT_PREC = const_1.Const.ULINT_SIZE, 
    s.BIGINT_PREC = const_1.Const.LINT64_SIZE, s.REAL_PREC = 4, s.DOUBLE_PREC = 8, s.DATE_PREC = 3, 
    s.TIME_PREC = 5, s.DATETIME_PREC = 8, s.DATETIME2_PREC = 9, s.TIME_TZ_PREC = s.TIME_PREC + 2, 
    s.DATETIME_TZ_PREC = s.DATETIME_PREC + 2, s.DATETIME2_TZ_PREC = s.DATETIME2_PREC + 2, 
    s.VARCHAR_PREC = 32767, s.VARBINARY_PREC = 32767, s.BLOB_PREC = const_1.Const.INT32_MAX, 
    s.CLOB_PREC = const_1.Const.INT32_MAX, s.NULL_PREC = 0, s.ROWID_PREC = const_1.Const.ROWID_SIZE, 
    s.LOCAL_TIME_ZONE_SCALE_MASK = 4096, s.BFILE_PREC = 512, s.BFILE_SCALE = 6, s.COMPLEX_SCALE = 5, 
    s.CURRENCY_PREC = 19, s.CURRENCY_SCALE = 4, s.LOCAL_DATETIME_SCALE_MASK = 4096, 
    s.ORACLE_FLOAT_SCALE_MASK = 129, s.ORACLE_DATE_SCALE_MASK = 8192, s;
}();

exports.DBType = DBType;